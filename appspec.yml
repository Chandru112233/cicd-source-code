version: 0.0
Resources:
  - MyLambdaFunction:
      Type: AWS::Lambda::Function
      Properties:
        Name: your-lambda-function-name
        Alias: live
        CurrentVersion:
          Fn::GetAtt:
            - MyLambdaFunction
            - Version
